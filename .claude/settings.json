{
  "permissions": {
    "allow": [
      "PowerShell(py -m pip install torch --index-url https://download.pytorch.org/whl/cpu 2>&1)",
      "PowerShell(py -c \"import torch, numpy; print\\('torch', torch.__version__, 'numpy', numpy.__version__\\)\" 2>&1)"
    ]
  }
}
